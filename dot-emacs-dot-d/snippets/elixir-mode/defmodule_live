# -*- mode: snippet -*-
# name: Live View Module
# key: dml
# --
defmodule $1.$2 do
  use $1, :live_view

  def mount(_params, _session, socket) do
    $0
    {:ok, socket}
  end

  def render(assigns) do
    ~L"""
    """
  end
end
